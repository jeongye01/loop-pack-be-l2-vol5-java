---
name: test-scenario-write
description: 요구사항을 테스트 파일에 배치하고 테스트 시나리오를 작성할 때 사용한다. docs/week2/requirements.md 의 요구사항을 알맞은 테스트 파일에 위치시키고, 요구사항마다 경계값 분석·동등 클래스 분할·의사결정표·상태 전이·오류 추측 중 알맞은 테스트 기법을 매핑해 시나리오를 작성한다.
---

# test-scenario-write

## 할 일

1. `docs/week2/requirements.md`의 요구사항(`R-…`)과 정책(`P-…`)을 알맞은 테스트 파일에 위치시킨다. 배치 결과는 `docs/week2/test-plan.md`의 `## 요구사항 배치` 표에 둔다.
2. 테스트 파일마다 배치된 요구사항에 아래 테스트 기법을 알맞게 매핑하고, 그 기법으로 시나리오를 작성한다. 결과는 `docs/week2/test-scenarios/<테스트 파일>.md`에 둔다.

## 테스트 기법

| 기법 | 이런 요구사항에 | 테스트할 값 |
| --- | --- | --- |
| 경계값 분석 | 범위가 있다 | 최소값, 최대값, 최소 바로 아래, 최대 바로 위, 명목값(선택) |
| 동등 클래스 분할 | 허용되는 입력과 거절되는 입력이 나뉜다 | 유효한 클래스와 유효하지 않은 클래스에서 대표값 하나씩 |
| 의사결정표 | 결과가 조건 여럿의 조합으로 정해진다 | 조건의 모든 조합 |
| 상태 전이 | 상태에 따라 같은 요청의 결과가 다르다 | 허용되는 전이와 막히는 전이 |
| 오류 추측 | 경험상 잘 깨지는 곳이 있다 | 필수값 비움, 잘못된 형식, 예상치 못한 행동 |

## 시나리오 문서 모양

```markdown
# StockTest 시나리오

## R-ORDER-08. 주문을 확정하려면 각 상품의 재고가 주문 수량 이상이어야 한다.

- 기법: 경계값 분석

| ID | 기법 | 시나리오 | 기대 결과 |
| --- | --- | --- | --- |
| STOCK-01 | 경계값 분석 | 재고 5에서 6을 차감한다 | 거절, 재고 5 그대로 |
```
